# LMS Backend

Minimal Express + MongoDB backend for the MERN LMS skeleton.

.env variables (see .env.example):
- MONGO_URI
- JWT_SECRET
- PORT

Install & run:

```bash
cd lms-backend
npm install
npm run dev
```

Seed sample data:

```bash
npm run seed
```
